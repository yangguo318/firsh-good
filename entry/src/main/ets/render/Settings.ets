export enum DisplayMode {
  Normal = 0,
  AOD = 1
}

export enum ClockStyle {
  Digital = 0,
  Minimal = 1,
  Hidden = 2
}

export enum KoiTheme {
  Orange = 0,
  Red = 1,
  Gold = 2,
  Mixed = 3
}

export enum PetalDensity {
  Sparse = 0,
  Normal = 1,
  Dense = 2
}
